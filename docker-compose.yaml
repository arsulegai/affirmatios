version: "3.6"

networks:
  affirmatios_agent_net:
    external: true

services:
  hospital:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hospital
    environment:
      - HTTP_HOST=0.0.0.0
      - HTTP_PORT=8080
      # TODO
    ports:
      - 9000:8080
    networks:
      - affirmatios_agent_net   
